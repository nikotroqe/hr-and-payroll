package com.example.hr_and_payroll.listener;

import com.example.hr_and_payroll.domain.entity.Attendance;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationEventPublisher;

public class AttendanceUpdatedEvent {
   /* @Autowired
    private ApplicationEventPublisher eventPublisher;

    public void updateAttendance(Attendance attendance) {
        // Update logic
        eventPublisher.publishEvent(new AttendanceUpdatedEvent(this, attendance));
    }*/
}
